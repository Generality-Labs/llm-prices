# Dependencies
node_modules/

# Wrangler local state (simulated D1/R2 data, dev caches)
.wrangler/

# Local secrets for `wrangler dev` — never commit
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# Build / coverage output
dist/
coverage/

# Generated from wrangler.toml by `wrangler types` (npm run typecheck)
worker-configuration.d.ts

# Playwright artifacts
test-results/
playwright-report/

# Claude Code per-user overrides (the shared .claude/settings.json is committed)
.claude/settings.local.json

# Editors
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
